<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kctsimplify.notification">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".NotificationView"
            android:label="Details of notification"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"/>
        </activity>


        <receiver android:name=".PlayerControllerBroadcast">
            <intent-filter>
                <action android:name="com.kctsimplify.notification.INTENT_PLAYPAUSE"/>
                <action android:name="com.kctsimplify.notification.INTENT_CANCEL"/>
            </intent-filter>
        </receiver>

        <service android:name=".NotificationService">
            <intent-filter>
                <action android:name="com.kctsimplify.notification.INTENT_OPENPLAYER"/>
                <action android:name="com.kctsimplify.notification.INTENT_PLAYPAUSE"/>
                <action android:name="com.kctsimplify.notification.INTENT_CANCEL"/>
            </intent-filter>
        </service>


    </application>

</manifest>